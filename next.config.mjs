/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["platform-lookaside.fbsbx.com", "lh3.googleusercontent.com"], // Cloudinary domain
  },
};

export default nextConfig;
